# LE REGISTRE DE L'ABI — ce que cette bibliothèque exporte, symbole par symbole.
#
# CE FICHIER EST LE CONTRAT (C12 du dépôt serveur). Cinq liaisons passent par
# ces symboles — Python, Ruby, C++, Kotlin, Swift —, elles vivent dans du code
# que nous ne voyons pas, et elles ne se mettent pas à jour au même rythme.
#
# `scripts/check-abi.sh` compare cette liste aux symboles RÉELLEMENT exportés par
# `libasl_client_ffi.so`, ET aux fonctions déclarées dans `include/asl.h`. Un
# ajout est libre mais doit être inscrit ici dans le même commit ; UN RETRAIT EST
# UNE RUPTURE MAJEURE et appartient à une version majeure, avec sa raison dans le
# message de commit.
#
# Une ligne par symbole. Les lignes vides et celles commençant par `#` sont
# ignorées.

# ── CE QUI NE TOUCHE À RIEN ─────────────────────────────────────────────────
asl_version
asl_faute_texte

# ── LA CONSTRUCTION ─────────────────────────────────────────────────────────
asl_client_neuf
asl_client_annuaire
asl_client_racines
asl_client_identite
asl_client_libere

# ── LES VERBES ──────────────────────────────────────────────────────────────
asl_enroler
asl_annoncer
asl_etat
asl_ou
